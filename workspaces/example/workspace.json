{
  "_comment": "Example workspace (settings-only). workspace_root resolves relative to the jumpcode's parent dir — point it at your project repo. Roles come from the central roles/ base set, overlaid by <workspace_root>/.jumpcode/roles/ if present. role_runtimes overrides a role's runtime, e.g. {\"backend-lead\": \"codex\"}.",
  "workspace_root": "workspaces/example",
  "role_runtimes": {}
}
